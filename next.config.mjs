// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  pageExtensions: ["ts", "tsx"],
  reactStrictMode: true,
  env: {},
};

export default nextConfig;
